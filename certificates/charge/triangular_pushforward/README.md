# Triangular Pushforward Certificate

This packet records the finite statement that pushforward along the
additive normal-ordered Gram grading preserves a supplied
positive/Cartan/negative direct-sum decomposition, with sign checked by
a supplied integer height.

Run:

```
python3 compute/verify_triangular_pushforward_fixture.py --fixture certificates/charge/triangular_pushforward --check
```

Expected status:

```
TRIANGULAR_PUSHFORWARD_VERIFIED
```

The packet does not prove exact triangular completion, target
triangular completion, compact-source primitive recognition,
no-extra relations, Pfaffian orientations, or protected traces.
