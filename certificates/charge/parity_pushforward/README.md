# Parity Pushforward Certificate

This packet records the finite super-vector-space statement that
pushforward along the additive normal-ordered Gram grading preserves
even and odd direct-sum blocks separately.

Run:

```
python3 compute/verify_parity_pushforward_fixture.py --fixture certificates/charge/parity_pushforward --check
```

Expected status:

```
PARITY_PUSHFORWARD_VERIFIED
```

The packet does not certify source parity, the GN/Kac parity equality,
compact source representatives, primitive recognition, Pfaffian
orientations, or protected traces.
