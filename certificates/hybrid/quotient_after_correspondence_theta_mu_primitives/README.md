# Quotient-after-correspondence theta_mu primitive compatibility

This packet records row 255 of the K3xE correction ledger.

It verifies
`QUOTIENT_AFTER_CORRESPONDENCE_THETA_MU_PRIMITIVES_VERIFIED` for the
finite primitive kernel and projection rows supplied to
Proposition `prop:quotient-after-correspondence-theta-mu-primitives`.
The certified statement is that the quotient comparison maps
`theta_mu_R` commute with the output primitive projection after the
row-254 coproduct square and supplied unit-counit descent identify the
primitive kernels.

The packet does not populate source compact Hall primitive matrices,
does not prove HN transition preservation of primitive subspaces, does
not prove protected-integration compatibility, and does not populate the
aggregate hybrid carrier.  The HN transition compatibility of \(Q_{E,R}\) is
supplied separately by
`certificates/hybrid/quotient_after_correspondence_hn_transition_compatibility`.
The protected integration definition is supplied separately by
`certificates/hybrid/protected_integration_definition`.
