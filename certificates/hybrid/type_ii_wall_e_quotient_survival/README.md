# Type-II wall E-quotient survival

This packet verifies row 269 at profile level.  It imports the row-268
hybrid operation-profile recovery packet and the
quotient-after-correspondence pseudofunctor packet.  For each simple
type-II wall profile, the quotient object assignment gives

```text
M_eta_i_R_wr_rig -> M_eta_i_R_wr_rig_slash_E.
```

For each of the `LW`, `WL`, and `LWL` operation profiles, the reduced
operation profile is the quotient of the preformed unreduced
correspondence stack.  Thus no type-II wall profile is lost by the
quotient-after-correspondence assignment.

This is not a proof of nonzero quotient cycles, coefficient descent,
orientation descent, the O2 atlas, protected integration, or aggregate
hybrid-carrier population.
