# Middle type-II wall wrapped/mixed routing

This packet verifies row 267 of the optimization ledger at routing
level.  The target wall image packet gives

```text
gamma_wall(delta_2) = (0,1,1).
```

On the type-II wall branch the elliptic degree is `d=m+1`, hence the
middle wall has `d=2>0`.  Under a retained branch-comparison row this
places the corresponding retained colour in `Gamma_R^wr`, not
`Gamma_R^loc`.  Local insertions against that wrapped colour are typed
by the one-sided `LW/WL` and ordered two-sided `LWL` mixed
correspondence definitions before the reduced `E`-quotient.

The packet does not supply retained wall objects, support-realization
rows, anchors, O2 wall charts, quotient descent, protected integration,
or aggregate hybrid-carrier population.
