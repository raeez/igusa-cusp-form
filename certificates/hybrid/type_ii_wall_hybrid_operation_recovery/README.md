# Type-II wall hybrid operation recovery

This packet verifies row 268 of the optimization ledger at operation
profile level.  The three imported simple wall images are

```text
gamma_wall(delta_1) = (1,1,0)
gamma_wall(delta_2) = (0,1,1)
gamma_wall(delta_3) = (0,-1,0)
```

On the type-II wall branch `d=m+1`, their degrees are `1, 2, 1`.
Thus each wall is routed to a singleton wrapped colour profile.  Local
insertions against any of these wrapped profiles are typed by the
`LW`, `WL`, and ordered `LWL` mixed correspondence definitions before
the reduced `E`-quotient.

This is not aggregate hybrid-carrier population.  It does not supply
retained wall objects, branch-comparison rows, support-realization
rows, anchors, mixed extension stacks, O2 wall charts, quotient
losslessness, protected integration, or transition rows.
