# Four-Input Pentagon Coherence

This packet records correction row 238.  It constructs retained
four-step flag-Quot common refinements for the sixteen length-four
local/wrapped words and proves the Mac Lane pentagon for the binary
hybrid product on those common refinements.

The proof uses the eight length-three associativity packets and the
BBDJS--Joyce--Upmeier Thom--Sebastiani orientation pentagon.  The
wrapped/wrapped base-change, projection-formula, and reduced
Thom--Sebastiani rows remain hypotheses wherever a \(WW\) edge enters
the four-input word.

This is not the higher-coloured tree category, not unit
compatibility, not quotient descent, not transition compatibility, and
not aggregate carrier population.

Run:

```sh
python3 compute/verify_four_input_pentagon_coherence_obstruction.py \
  --fixture certificates/hybrid/four_input_pentagon_coherence --check
```

Expected status:

```text
FOUR_INPUT_PENTAGON_CONDITIONAL_VERIFIED
```
