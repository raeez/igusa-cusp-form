# rhomred_weyl_partial_action_groupoid

This packet verifies row 308 as a finite groupoid-datum criterion.  A
populated \(\mathcal G_R\) requires retained quotient-orientation
objects, type-II wall arrows carrying supplied \(\tau_i\) maps,
identities, inverses, partial compositions, and zero source-target,
inverse, and associativity defects.

The present packet is fail-closed: target type-II generators and
quotient wall profiles are available, but the finite groupoid tables are
empty.

Run:

```sh
python3 compute/verify_rhomred_weyl_partial_action_groupoid.py \
  --fixture certificates/orientation/rhomred_weyl_partial_action_groupoid --check
```
