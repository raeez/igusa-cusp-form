This packet records the row 339 criterion for proving that orientation
classes survive the inverse limit.

It is intentionally not a proof of survival.  The reduced-orientation
packet has no finite orientation-line rows; the square-root packet has
only missing square-root obstruction rows; and the transition-orientation
packet has no Picard pullback maps, strict composition rows, or
orientation Mittag-Leffler row.

The verifier checks that the criterion is recorded and that the current
source packets remain obstruction ledgers.  It also keeps row 340,
``no new orientation class appears at the limit'', separate.

Status:

`RHOMRED_ORIENTATION_CLASS_LIMIT_SURVIVAL_OBSTRUCTION_VERIFIED`
