This packet verifies row 330 as the chart-level orientation criterion
for the isotropic a_ij charts.

The target type-II chamber and W<=3 parity packets record three
primitive isotropic rays a_12, a_13, a_23, each with target parity
10|0 and decomposition 10 = 1 + 9.  If a compact Hall source chart
realizes the ten directions as even oriented summands with no odd
summand, the local determinant-orientation parity exponent is zero and
the chart sign is +1.

The present packet records only this criterion.  It does not construct
compact Hall isotropic charts, quotient orientations,
determinant-square rows, source parity rows, or transition rows.
