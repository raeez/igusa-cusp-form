# Row 319 orientation character definition

This packet records the finite criterion for row 319 of the K3xE
optimization ledger: define the orientation character epsilon_o from
the strictified source-side Weyl transport.

The packet is intentionally an obstruction packet. It imports the
row-312 Coxeter-cochain transition packet, the row-314 torsor-defect
vanishing packet, the row-315 through row-318 component-preservation
packets, and the automorphic Maass character packet as a firewall. The
present data do not contain strict Weyl action rows, generator-sign
rows, stratum-independence rows, transition-compatible character rows,
or inverse-limit character rows.

The verifier checks that the automorphic Maass character, determinant
character, target Coxeter graph, local Pfaffian sign, OP scalar branch,
or empty sign table is not read as the source-side orientation
character.
