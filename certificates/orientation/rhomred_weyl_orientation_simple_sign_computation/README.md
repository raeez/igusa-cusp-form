# Row 320 simple orientation-character signs

This packet records the finite criterion for row 320 of the K3xE
optimization ledger: compute the source-side generator values
epsilon_o(s_delta_i) = -1.

The packet is intentionally an obstruction packet. It imports the
row-319 orientation-character definition packet, the O2 wall-atlas
ledger, the finite Pfaffian ledger, and the automorphic Maass character
packet as a firewall. The present data do not contain retained wall
objects, rank-one wall charts, invariant Pfaffian units, coordinate
flip rows, local sign rows, generator-sign rows, stratum-independence
rows, or transition-compatible sign rows.

The verifier checks that the automorphic value nu_Delta5(s_delta_i) =
-1, the determinant character, the OP scalar branch, a squared
determinant, or an empty wall-chart table is not read as the
source-side computation of epsilon_o(s_delta_i).
