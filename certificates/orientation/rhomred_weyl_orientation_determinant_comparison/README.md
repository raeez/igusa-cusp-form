This packet records the row 321 criterion for comparing the
source-side orientation character epsilon_o with the determinant
character on W^(2).

It verifies only the group-theoretic criterion:

- row 319 must construct a source-side orientation character;
- row 320 must supply the three source generator signs
  epsilon_o(s_delta_i) = -1;
- the determinant character has det_W(s_delta_i) = -1 for the three
  type-II simple reflections;
- equality on generators then gives epsilon_o = det_W on W^(2).

The packet deliberately records no global comparison theorem.  The
current row-319 packet has no constructed character, and the current
row-320 packet has no finite generator-sign rows.  The Maass character
packet is imported only as target automorphic determinant data; it is
not a source orientation-character computation.
