# Retained Rigidification And Residual Inertia Certificate

This packet proves item 169 at finite HN height.  For each retained
derived enhancement it records a scalar rigidification: the inertia
group has scalar subgroup `G_m`, the scalar subgroup is removed, and
the residual group is finite with zero rigidification defect.

It certifies only residual finiteness after scalar rigidification.  It
does not construct universal complexes, finite closed inertia
stratifications, extension/flag stacks, cosection atlases, transitions,
Pfaffian orientations, or protected traces.

Run:

```sh
python3 compute/verify_retained_rigidification_inertia_fixture.py --fixture certificates/moduli/retained_rigidification_inertia --check
```
