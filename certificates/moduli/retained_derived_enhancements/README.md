# Retained Derived Enhancements Certificate

This packet proves item 168 at finite HN height: every retained
finite-type semistable substack has a quasi-smooth derived Artin
enhancement with cotangent amplitude `[-1,0]`, zero Tor-amplitude
defect, and a PTVV `(-1)`-shifted symplectic form with zero symplectic
defect.

It certifies only the derived-enhancement row.  It does not construct
universal complexes, rigidifications, finite residual inertia,
extension/flag stacks, cosection atlases, transitions, Pfaffian
orientations, or protected traces.

Run:

```sh
python3 compute/verify_retained_derived_enhancements_fixture.py --fixture certificates/moduli/retained_derived_enhancements --check
```
