# Third Hybrid Attack-Heal Report

Date: 2026-04-28.
Worktree: `/tmp/igusa-third-hybrid`.
Branch: `agent/igusa-third-hybrid-20260428`.

## Claim Attacked

The attacked failure mode was that the hybrid wrapped repair could still
be read as ordinary \(\operatorname{Ran}(E)\) factorization with an
extra degree label or a detached global Fock/Hecke determinant factor.

Status: false.  Positive elliptic degree is not a scalar decoration of
the projection-finite Ran sector.  It requires finite-stage categorical
memory before quotienting by \(E\): rigidified wrapped prequotients,
anchors, mixed and wrapped extension correspondences, two-step flag
coherences, quotient-after-correspondence functoriality, and protected
integration whose \(s\)-degree is forced by \(b_R\).

## Manuscript Inscription

- `main.tex:228-236` now states the \(s\)-direction as finite-stage
  local/wrapped correspondence data.  A detached Hecke/Fock factor is
  only determinant-level unless represented by wrapped prequotients and
  mixed Hall actions before the reduced \(E\)-quotient.
- `main.tex:444-452` replaces the old local/global repair language by
  the finite categorical repair: local sector, rigidified wrapped sector,
  mixed/wrapped correspondences, quotient-after-correspondence descent,
  and protected integration.
- `main.tex:4799-4820` strengthens
  \(\operatorname{Ran}^{\mathrm{hyb}}(E)\): it is a finite-coloured
  correspondence system with local supports, wrapped colours, rigidified
  wrapped objects, and anchor values.  The disjoint union
  \(\bigsqcup_b\operatorname{Ran}(E)_{[b]}\) is only its degree shadow.
- `main.tex:4861-4884` adds
  \(\mathsf{Corr}^{E,\mathrm{hyb}}_R\) to the finite hybrid certificate.
- `main.tex:5008-5020` forces wrapped-wrapped correspondences to retain
  the three anchor values
  \((\lambda_{\eta_1,R},\lambda_{\eta_2,R},
  \lambda_{\eta_1+\eta_2,R})\) before quotienting.
- `main.tex:5022-5035` defines the finite \(E\)-equivariant
  correspondence category generated by the local, mixed, wrapped, and
  flag stacks; operations are its Borel--Moore pull-push images.
- `main.tex:5091-5124` makes \(Q_{E,R}\) a supplied functor on the
  finite correspondence module category, with comparison isomorphisms
  \(\theta^Q_{\mu,R}\) coherent for flags and transitions.
- `main.tex:5126-5168` upgrades protected integration to a finite-stage
  character for the reduced operations and records the multiplicative
  law
  \[
  s^{b_R(\gamma_1+\cdots+\gamma_k)}
  =\prod_i s^{b_R(\gamma_i)}.
  \]
- `main.tex:5213-5263` updates the consequences: a decoupled
  Fock/Hecke factor without
  \(\mathsf{Corr}^{E,\mathrm{hyb}}_R\), anchored mixed
  correspondences, flag coherence, and \(Q_{E,R}\)-descent does not
  define the primitive brackets.
- `main.tex:5294-5298` replaces the slogan "structural certificate and
  construction target" by the precise status: finite categorical datum
  whose existence remains open.
- `main.tex:6899-6919` and `main.tex:8328-8336` propagate the same
  finite correspondence-category requirement into \((H_X)\) and the
  final synthesis.

## Status

Conditional certificate only.  The manuscript now states the finite data
needed for the hybrid lane and prevents a quotient-first or detached
Fock-factor reading.  It still does not construct the compact Hall
category, the rigidified wrapped prequotients, the extension stacks, the
orientation system, \(Q_{E,R}\), protected integration, or the primitive
Borcherds--Kac--Moody recognition.

Primary source anchors were not newly needed in this pass: the repair is
an internal categorical scoping of the existing compact-realization
certificate.  Existing Borcherds, Gritsenko--Nikulin, Beilinson--
Drinfeld, and Oberdieck--Pixton citations remain the external anchors for
the determinant, denominator, chiral Koszul, and OP scalar lanes.

## Checks

- `rg -F` confirmed no remaining `hybrid local/global`, `local/global`,
  `structural certificate`, or `construction target` phrase in
  `main.tex`.
- `git diff --check` passed.
- `pdflatex -halt-on-error -interaction=nonstopmode
  -output-directory=/tmp/igusa-third-hybrid-texcheck main.tex` passed.
  It was a one-pass syntax check, so unresolved references/citations and
  rerun warnings remain expected.

## Remaining Obligations

1. Construct the compact extension-closed Hall category and finite HN
   quotients.
2. Construct finite-type rigidified wrapped prequotients and anchors
   \(\lambda_{\eta,R}\).
3. Construct local-local, local-wrapped, and wrapped-wrapped extension
   stacks with admissible vanishing-cycle pull-push functors.
4. Prove flag associativity and orientation coherence in every finite
   quotient.
5. Construct \(Q_{E,R}\) as a quotient-after-correspondence functor with
   coherent \(\theta^Q_{\mu,R}\).
6. Construct protected integration recovering the \(s^{b_R}\) trace law
   coefficientwise and prove the primitive BKM presentation.
