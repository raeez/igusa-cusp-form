# Sixteenth reconstitution attack-heal ledger -- 2026-04-28

## Scope

Fresh post-merge adversarial pass on the manuscript proper after
`ab0ea07`.  Twenty read-only lanes attacked compact source construction,
hybrid Hall convolution, Mittag--Leffler descent, orientation data,
NO7, K3-to-E specialization, source/product exponents, cross-repo
consistency, compute boundaries, dependency closure, stale notation,
formality, scalar/Pfaffian signs, sectorial finite-HN language,
primitive recognition, boundary/eight-row catalogue, and git topology.

## Integrated repairs

- Propagated Koszul Mittag--Leffler exactness into the introduction,
  the \(D_X\) certificate, the primitive-recognition theorem, and the
  full-certificate checklist.
- Strengthened finite-stage wording: charge-index finiteness,
  finite-type substacks, finite-dimensional protected cohomology, and
  proper/admissible pull-push geometry are separate entries of the same
  finite Hall certificate.
- Replaced stale "finite Hall quotient" language by finite
  charge-support Hall quotient language.
- Kept the \(W_{\le3}\) primitive-recognition window as the first
  saturated finite test; removed old `R_min` and ASCII `W_{<=3}` roadmap
  notation from current notes.
- Reasserted that the parity table uses the imported
  Gritsenko--Nikulin/Kac convention and that target computations do not
  supply source parity bases.
- Routed cosection parity coherence through the supplied \((O1)\)
  residual rather than through scalar normalization.
- Made protected integration homogeneous for the full Gram label
  \(\overline\Pi_X(\gamma)=(n,l,m)\), with \(s^{b_R(\gamma)}\) recording
  the elliptic degree.
- Restated the smooth-\(S\), smooth-\(E\) compact-stage scope near the
  compact realization and Ran-local openings; kept singular K3 limits,
  nodal/cuspidal \(E\), imprimitive classes, and the compact Hall
  \(s=0\) cusp as boundary certificates.
- Added source support for Thom--Sebastiani, \(E_n\)-formality,
  Mukai lattices/moduli, Bridgeland-type stability, Maulik--Toda,
  Costello--Li, and Cao--Gross--Joyce references.

## Stable attacks

- No nonempty compact \(E_3\) source is constructed.  Open Problem 5427
  remains the gate to an unconditional compact realization theorem.
- Hybrid Hall convolution remains a supplied finite-stage certificate:
  properness, Thom--Sebastiani transversality, orientation, finite
  fibres, and d-critical/cosection data are not generated by the
  determinant.
- NO7 remains a finite source-side Hopf-radical test requiring actual
  Hopf pairing, coproduct, and radical matrices.
- The Borcherds product, Gritsenko--Nikulin denominator algebra, Maass
  character, OP scalar square, and compute scripts remain target-side
  checks; they are not compact source multiplicities.
- Cross-repo audit found the local manuscript correctly fenced.  Vol III
  contains stronger theorem-grade Hall--Borcherds quotient language that
  should not be imported here without the primitive representatives,
  radical/coideal, PBW, orientation, and Hall correspondence certificate.

## Worktree audit

All local and remote branch tips are merged into `master`; `origin/master`
and local `master` were both `ab0ea07` before this pass.  The registered
old agent worktrees still contain uncommitted residue.  Those diffs are
not branch-tip merge failures, but they are not silently discarded:
future cleanup must either archive or semantically retire them worktree
by worktree.  Tracked `out/main.pdf` is modified in many auxiliary
worktrees and must be handled separately from ignored TeX outputs.
