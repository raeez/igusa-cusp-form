# Worktree Agent D Weyl/Pfaffian Report

Date: 2026-04-28.

## Claim Attacked

The attacked claim was the possible promotion of automorphic data to a
geometric Hall orientation theorem.  The repaired manuscript separates:

- pure group theory: the type-II reflection group has Coxeter
  presentation
  \(s_{\delta_1}^2=s_{\delta_2}^2=s_{\delta_3}^2=1\), hence
  \(W^{(2)}(\Lambda^{2,1}_{II})_{\mathrm{ab}}\simeq(\mathbb Z/2)^3\);
- geometric determinant-line monodromy: `(O1)+` is now an honest
  Weyl-equivariant transport datum for the orientation/Pfaffian line;
- local geometry: `(O2)` is a finite three-wall Pfaffian chart
  certificate, still a hypothesis until those charts are constructed;
- automorphic target: Maass/Gritsenko--Nikulin compute
  \(\nu_{\Delta_5}\), without using scalar normalization.

## Constants And Signs

Type-II simple walls:

| root | Gram triple | coefficient | Pfaffian modes | Hall sign under O2 | Maass target |
|---|---:|---:|---:|---:|---:|
| \(\delta_1=2f_2-f_3\) | \((1,1,0)\) | \(f(0,1)=1\) | \(N_{\delta_1}=1\) | \(-1\) | \(-1\) |
| \(\delta_2=2f_{-2}-f_3\) | \((0,1,1)\) | \(f(0,1)=1\) | \(N_{\delta_2}=1\) | \(-1\) | \(-1\) |
| \(\delta_3=f_3\) | \((0,-1,0)\) | \(f(0,-1)=1\) | \(N_{\delta_3}=1\) | \(-1\) | \(-1\) |

The type-II Cartan matrix is
\[
\begin{pmatrix}2&-2&-2\\-2&2&-2\\-2&-2&2\end{pmatrix}.
\]
All off-diagonal Coxeter exponents are infinite, so any three signs on
the generators extend uniquely to a character.

The chamber-permuting roots
\[
f_{-2}-f_2,\qquad f_2-f_3,\qquad f_2+f_3
\]
have automorphic Maass value \(+1\).  In the type-II product
polarization their divisor order is zero: the primitive roots are not
type-II product roots; \(2(f_{-2}-f_2)\) gives triples outside
\(\Gamma_{\mathrm{eff}}\), while \(2j(f_2\mp f_3)\) gives
\((j,\pm2j,0)\), and \(f(0,\pm2j)=0\) for \(j\ge1\).  No Hall
orientation sign is asserted on these reflections without a separately
supplied semidirect-product Hall lift.

The constants \(64\) and \(-4096\) are not used in the character
verification.  They remain scalar normalization data and are
orientation-forgetful.

## Files Changed

- `main.tex`
- `notes/worktree_weyl_pfaffian_report.md`
- `out/main.pdf` regenerated by `make`

## Commands Run

- `sed`/`nl` reads of `.agents/skills/chriss-ginzburg-rectify/SKILL.md`,
  `CLAUDE.md`, `AGENTS.md`, ecosystem invariants, relevant notes,
  appendices, compute scripts, and the assigned `main.tex` regions.
- `rg` scans for `(O1)+`, `(O2)`, Pfaffian, Maass, orientation,
  scalar-normalization, type-II, and `Aut(\Poly_{II})` anchors.
- `python3 compute/verify_lattice.py`
- `python3 compute/verify_square_root.py`
- Inline Python check of the type-II Cartan matrix, Gram triples, wall
  divisor orders, and Aut-chamber zero-order computation.
- `git diff --check`
- `git diff --cached --check`
- `make`

## Theorem Status

Proved in the manuscript: the sign-extension lemma is pure group theory.
Given an honest type-II Weyl action on the orientation/Pfaffian line, the
three generator signs determine a unique character on
\(W^{(2)}(\Lambda^{2,1}_{II})\).

Conditional in the manuscript: the Hall signs
\(\varepsilon_o(s_{\delta_i})=-1\) require `(O2)`, now formulated as a
finite three-wall Pfaffian chart certificate.

Unconditional target data: the Maass character has value \(-1\) on the
three type-II simple reflections and \(+1\) on the three
\(\Aut(\Poly_{II})\)-reflections.

## Remaining Obstruction

Construct the reduced compact Hall orientation line, prove the
Weyl-equivariant determinant-line transport `(O1)+`, and build the three
local Pfaffian charts of Definition
`def:type-ii-pfaffian-wall-certificate`.  Until then the equality
\(\varepsilon_o=\nu_{\Delta_5}\) is a conditional certificate theorem,
not a constructed geometric theorem.
