# Fourteenth reconstitution attack-heal: manuscript proper

Date: 2026-04-28.

Scope: six-agent adversarial attack on the reconstituted manuscript
proper, followed by lossless integration into `main.tex` and `proj.bib`.
Main integration owner: current thread.  Agents supplied evidence only;
truth was integrated by direct check against the manuscript, computations,
and source anchors.

## Agents and lanes

1. A14-STATUS-STANDALONE: attacked process language, supplied/open status,
   and standalone proof-strength phrasing.
2. A14-LATTICE-NORMALORDER: attacked `Gamma_X^{phys}` typing, finite
   normal-ordering labels, and no-overcount residuals.
3. A14-ORIENTATION-WEYL: attacked finite Borel descent, Weyl action
   groupoid typing, and section-level Pfaffian equality.
4. A14-HYBRID-O2-KOSZUL: attacked the O2 wall-atlas package, hybrid
   transition notation, colored integration residuals, and augmented
   source-bar notation.
5. A14-D0-PFAFFIAN-RECOGNITION: attacked the Pfaffian positive half,
   parity-explicit Pfaffian factors, and finite-window primitive
   recognition for the full BKM double.
6. A14-AUTOMORPHIC-SOURCE-CROSS: attacked Igusa primary attribution and
   CHL scalar-square row labels.

## Surviving attacks and repairs

- Status language: removed time-bound language from the first-order
  certificate and finite normal-ordering closure statement.  Supplied maps
  in the quantum comparison remark are now displayed as actual supplied
  quasi-isomorphisms, not `?` maps.
- Lattice and normal ordering: `Gamma_X^{phys}` is explicitly identified
  with the formal full-Mukai bookkeeping lattice; finite normal-ordered
  primitive layers are typed by `(c,T)`; the no-overcount residual is a
  cone whose acyclicity is required; semistable generators in `(c,0)` are
  separated from composite HN labels.
- Orientation and Pfaffian comparison: finite stabilizer descent now uses
  Borel classes before point-stabilizer reductions; Weyl and semidirect
  obstruction classes are action-groupoid classes unless a complete orbit
  is supplied; section equality uses the q-expansion principle after the
  supplied automorphic line comparison.
- O2 and hybrid: `(O2)` now requires the full type-II wall atlas package
  for global Weyl transport.  Unreduced hybrid transitions
  `tau^{E,hyb}` are separated from reduced chain maps `bar tau^{hyb}`.
  The hybrid obstruction tuple includes the colored protected-integration
  residual `o_R^{I,col}`.
- Koszul lane: source bar construction is explicitly the augmented bar of
  the supplied hybrid source, with length pieces generated by the
  augmentation ideal.
- D0/Pfaffian and recognition: compact parity lifts in the open
  D0-Pfaffian obligation are restricted to the cusp-positive test window;
  negative representatives are deferred to primitive recognition.  The
  Pfaffian block now spells out even and odd contributions.  The cofinal
  recognition certificate is triangular and contains an exact-completion
  clause, so cancellation words are not silently made finite data.
- Automorphic/source cross-check: Igusa 1962 and 1964 primary references
  are now in `proj.bib` and cited at the normalized cusp-form and
  Fourier-Jacobi coefficient anchors.  CHL scalar squares for `nabla_3`
  and `nabla_2` now use the sourced labels `Phi_6^{(1,2)}` and
  `Phi_4^{(1,3)}`; the `F_7^2=Phi_3` claim is marked as the composite
  `N=4` scalar form.

## Lossless side-work merge audit

After the A14 repairs, a fresh non-destructive audit was run against:

- 78 registered git worktrees;
- 80 local branch heads;
- 99 unreachable commits containing `main.tex`;
- the current uncommitted `main.tex`.

The label-level audit found zero labels present in any audited
side-tree, worktree, branch, or unreachable `main.tex` that are absent
from the current manuscript.  This does not promote old overclaims; it
certifies that their labelled mathematical lanes are either present,
subsumed in stronger form, or recorded as conditional/rejected in the
current manuscript.

## Verification

- `python3 compute/verify_square_root.py` passed.
- `python3 compute/verify_lattice.py` passed.
- `git diff --check -- main.tex proj.bib` passed.
- `make` rebuilt `out/main.pdf`.

## Remaining open obligations

- Construct the compact Hall source kernels, hybrid wrapped certificates,
  O2 wall atlases, and finite Hall--Dirac atlases.
- Derive the Igusa exponents from source correspondences rather than
  testing supplied source parity data against the imported Borcherds
  product.
- Prove the full positive/negative primitive-recognition package with
  exact triangular completion, radical quotient, PBW compatibility, and
  full parity dimensions.
