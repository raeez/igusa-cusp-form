# Architecture Realization Status

Date: 2026-04-28.

Internal ledger.  The manuscript remains standalone and does not cite
this note.

## Realized in `main.tex`

1. The opening now states the Dirac principle: the square root is a
   Pfaffian/first-order problem, not an analytic branch of
   `Delta_5^2`.
2. The D6--D2--D0 dictionary now belongs to the physical-charge part.
   The Mukai vector computation is the CY3 computation and gives
   `Pi_X(Q_Y,P_Y)=(h-1,n,d-1)`.
3. The physical charge lattice, the Gram lattice, and the corrected
   extension are separated:
   `Gamma_X^phys`, `Gamma_gram`, and
   `widehat Gamma_X=Gamma_X^phys oplus_B Gamma_gram`.
4. The normal-ordered map
   `overline Pi_X(c,T)=Pi_X(c)-T` is theorem-level and central.
5. Raw Gram descent is ruled out by
   `thm:raw-gram-descent-no-go`.
6. The chain-level descent notation is now
   `overline Pi_{X,*}^Theta`, not raw `Pi_{X,*}^Theta`.
7. The compact realization is a five-level Dirac-Igusa certificate
   `(L_X,H_X,O_X,D_X,R_X)`.
8. The compact-realization part now separates observables from states:
   the formal current envelope is a target observable algebra, not a
   compact BPS state space.
9. The OP/DT square is imported and orientation-forgetful.  The OP
   scalar sign is not used as Hall orientation monodromy.
10. Boundary rows are treated as independent certificates.  The spin
    `L`-factor note is quarantined in the appendix.
11. The finite `D0` certificate now separates HN height from the
    automorphic target window, uses a cofinal `N_R^Delta` schedule, and
    requires finite null-trivializations.
12. Orientation now separates degree-two gerbe obstruction classes from
    degree-one finite-stabilizer linearization characters.
13. The type-II Pfaffian lane separates local normal-mode ranks
    `N_delta^Pf` from automorphic divisor orders `d_delta^aut`.
14. The hybrid object now has ordered two-sided local/wrapped
    correspondences, the full eight-word flag atlas, quotient residuals,
    and protected integration residuals.
15. Chain-level normal ordering now carries finite-fibre HN topology,
    `mathfrak o_Theta^top`, finite-window radical checks,
    no-extra-relations checks, and PBW checks.
16. The first timelike recognition test is now computed two ways:
    `[qrs]D_5=93`, `m(delta_1+delta_2+delta_3)=-93`, and the
    presentation split `29|93`.
17. The finite `D0` lane now uses a cofinal discrete HN subsystem,
    successor transition defects, finite operators `mathfrak D_{X,R}`,
    finite Pfaffian square data, and a proof that the automorphic product
    supplies only a `K_0` Pfaffian shadow, not compact parity pieces.
18. The Weyl orientation lane now has an explicit quotient-orientation
    obstruction package: projective determinant-line cocycle, generator
    square constraints, and finite-stabilizer torsor defects.
19. The Pfaffian local wall lane now states the exact geometric local
    calculation needed on each type-II wall: equivariant chart, reduced
    self-Ext splitting, invariant normal Pfaffian unit, and
    `N_delta^Pf=1`.
20. The hybrid source lane now includes quotient and protected-integration
    transition residuals and rules out quotient-first or determinant-only
    replacements.
21. The Koszul source lane now requires a coaugmented finite source
    coalgebra with counit, finite filtration, collision coproduct,
    coassociativity, counit, conilpotence, and transition-continuity
    defects.
22. The primitive recognition lane now has the first saturated finite
    window `W_{<=3}` and next timelike additive coefficient checks
    `90,90,90,-540` generated by `compute/verify_square_root.py`.
23. The finite `D0` lane now has the stronger finite Hall--Dirac atlas
    `A_R`, exact Pfaffian square-root torsor obstruction in
    `Pic(Q_R)[2]`, compact parity-lift monoids, and a no-hidden-cancellation
    axiom explicitly marked as extra source data.
24. The orientation lane now has the full finite-stabilizer obstruction
    to `(O1)`: odd `E[N]` transfer vanishing, the `E[2]`
    `(r_i;rho_i)` tuple, and higher even
    `(A_1,A_12,A_2;lambda_1,lambda_2)` residuals.
25. The Weyl lane now states the semidirect wall-transport obstruction:
    Weyl equivariance requires a lift of the determinant-line transport
    to the reduced finite quotient, not only the Maass target character.
26. The `(O2)` lane now requires compatibility between the three type-II
    local Pfaffian wall models and the same finite hybrid correspondence
    system.  The `delta_2` wall has positive elliptic degree and cannot
    come from a projection-finite Ran sector.
27. Chain-level normal ordering now distinguishes the formal central
    translation cochain from its compact Hall realization, and records the
    finite verification diagrams `(NO1)--(NO7)`.
28. The Koszul lane now has a canonical finite source-bar coalgebra once
    the hybrid source is supplied, plus the remaining source-to-target
    cobar comparison obstruction.
29. The primitive recognition lane now includes height-four signed
    residuals `108|90|18`, doubled isotropic residuals `10|9|1`, and
    real-string exponent checks generated by
    `compute/verify_square_root.py`.
30. The older determinant/Hall-constant no-go is now an explicit
    proposition in the primitive-recognition lane.
31. The older two-torsion closure attempts are preserved as conditional
    or rejected routes, not as unconditional theorems.  The surviving
    statement is the finite Klein-four criterion plus trivial
    `H^1(BE[2];F_2)` linearization character.
32. The old formal-current and eight-row theorem labels are represented
    by the current denominator-algebra, formal-current, primitive
    recognition, and boundary-current statements, so branch/worktree
    label audit is closed without changing claim strength.

## Still Open Mathematics

1. Construct the compact first-order Dirac-Igusa certificate `D0`.
2. Construct strong reduced orientation `(O1)` on the compact/wrapped
   sector.
3. Prove the Weyl-equivariant orientation cocycle `(O1)+`.
4. Compute the Pfaffian local model `(O2)` geometrically on the
   type-II walls.
5. Prove the trivial `E[2]`-linearization and the higher even `E[N]`
   linearization characters, not only the degree-two gerbe bits.
6. Construct the hybrid local/wrapped factorization object
   `mathcal F_X^hyb`, including all ordered mixed correspondences,
   quotient residuals, and protected integration residuals.
7. Lift lattice normal ordering to the full chain-level Hall descent.
8. Prove primitive recognition at presentation level against
   `mathfrak g_{Delta_5}`, including finite-window radical,
   no-extra-relations, and PBW theorems.

These are not architecture gaps.  They are the mathematical construction
targets isolated by the architecture.  The sixth swarm sharpened them
again: the manuscript now records the finite obstruction packages whose
vanishing would turn each open target into an actual construction.

## Branch / worktree merge status

After the latest semantic merge pass, every theorem/lemma/proposition
label present in the registered git worktrees and local branches exists
in current `main.tex`.  Where the older branch statement was too strong,
the current manuscript preserves the mathematical content as a conditional
route or a rejected shortcut, not as a false theorem.
