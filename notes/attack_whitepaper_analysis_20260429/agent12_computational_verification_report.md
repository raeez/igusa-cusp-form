# Agent 12 Computational Verification Report

Scope: computational and coefficient obligations in the 2026-04-29 attack transcript, compared against `main.tex`, `proj.bib`, and `compute/`.  No edit to `main.tex`.

## Verification Table

| computed claim | current anchor | command/check available | result if run | unresolved computation | required script/table additions | rewrite implications |
|---|---|---|---|---|---|---|
| `phi_{0,1}` normalization: `Z_K3=2 phi_{0,1}`, `phi_{0,1}=sum f(n,l)q^n r^l`; first rows are `(r^-1+10+r)`, `q(10r^-2-64r^-1+108-64r+10r^2)`, `q^2(r^-3+108r^-2-513r^-1+808-513r+108r^2+r^3)`. | `main.tex:722-740`, `main.tex:13960-13998`; `proj.bib:87-92` for Eichler--Zagier. | `python3 compute/verify_square_root.py`; assertions at `compute/verify_square_root.py:415-430`. | Passed. Printed the `q^0`, `q^1`, `q^2` rows exactly. Script also asserts coefficients through `q^4`. | No primary-source coefficient audit in this pass; script derives from theta series, not from EZ/GN page images. | Stable. Keep coefficient convention explicit. Do not turn negative coefficients into negative dimensions; current `K_0`/superdimension language is correct. |
| Theta-to-monic normalization: `[q^{1/2}r^{1/2}s^{1/2}] Delta_5=64`, `D_5=64^{-1}Delta_5`. | `main.tex:73-81`, `main.tex:321-328`, `main.tex:13752-13759`, `main.tex:13868-13876`; `proj.bib:100-134` for GN/GNII. | No standalone script computes the leading theta constant; it is imported. Arithmetic conversion checked by scripts using `D_5` as monic product. | Local arithmetic consistent. `4096=64^2` used correctly in OP conversion. | Direct GN/GNII published-equation audit still needed before lock. | Keep `Delta_5` theta-normalized and `D_5` monic. Avoid GN notation drift where GNII may write the monic lift as `Delta_5`. |
| Borcherds product exponents: after the leading monomial, exponents are `f(nm,l)` on the active product support. | `main.tex:1946-1956`, `main.tex:5396-5414`, `main.tex:13998-14012`; `compute/verify_square_root.py:168-207`. | `python3 compute/verify_square_root.py`; product coefficient code in `monic_delta_coefficient`. | Passed for all hard-coded low-degree checks. | No general arbitrary-order product verifier or GN source-level theorem checker. | State "visible positive signed root supermultiplicities on active support." Do not imply absence of zero-superdimension root spaces or parity splits. |
| First `qrs` coefficient after removing the leading monomial: `[qrs] D_5/(qrs)^{1/2}=93`. | `main.tex:12838-12915`; `compute/verify_square_root.py:204-207`, `433-435`. | `python3 compute/verify_square_root.py`. | Passed. Printed `93`. Laurent check printed `9r^-3-93r^-2+90r^-1-90+93r-9r^2`, so the `r^1` coefficient is `93`. | None for target arithmetic. Compact source interpretation unresolved. | Safe as target-side Borcherds/GN coefficient. Do not call it a compact Hall primitive count. |
| First timelike presentation split: `m(delta_123)=-93`, target dimensions `29|93`, signed difference `29-93=-64=f(1,1)`. | `main.tex:12415-12422`, `main.tex:12739-12749`, `main.tex:12838-12939`; `compute/verify_square_root.py:401-410`, `433-438`. | `python3 compute/verify_square_root.py`. | Passed. Printed `29|93` and `m(delta_1+delta_2+delta_3)=-93`. | No compact source bases, Hall constants, pairing matrices, radical kernels, or PBW data. | Keep as target presentation data. The source must still exhibit 29 even and 93 odd compact primitives plus structure maps. |
| Low-degree active rows through height three. | `main.tex:12633-12658`. | `python3 - <<'PY' ...` using `phi_01_coefficients`, `delta_pair`, `signed_root_supermultiplicity`. | Passed. Reproduced the displayed `W_{\le3}` active rows: three real roots with signed multiplicity `1`, three isotropic rows with `10`, six height-three real rows with `1`, and `delta_123` with `-64`. | This is bounded target enumeration, not a proof of compact source realization. | Add a printed/table mode to `compute/verify_square_root.py` for the `W_{\le3}` table so the manuscript table has a canonical command. | Current table is numerically consistent. Mark it as denominator target data, not Hall source data. |
| Root-string constants: distinct real roots have pairing `-2` and exponent `3`; complementary real root against isotropic `a_ij` has pairing `-4` and exponent `5`; `delta_i` against `delta_123` has pairing `-2` and exponent `3`. | `main.tex:12675`, `main.tex:12723-12728`, `main.tex:12793-12799`, `main.tex:13145-13155`; `compute/verify_square_root.py:236-254`, `305-321`, `459-468`. | `python3 compute/verify_square_root.py`. | Passed. Printed `real_real_pairing=-2`, `real_real_exponent=3`, `complement_isotropic_pairing=-4`, `complement_isotropic_exponent=5`, `timelike_delta123_pairing=-2`, `timelike_delta123_exponent=3`. | Source-side real-string maps are not computed. | Add a root-string table output keyed by `(root, other, pairing, Serre exponent)` for direct manuscript comparison. | Keep no-go theorem and normal-ordering necessity. These constants support target relations only. |
| Type-II lattice matrix, Weyl vector, and exponential dictionary. | `main.tex:4883-4911`, `main.tex:4931-4938`, `main.tex:5388-5393`; `compute/verify_lattice.py:92-125`. | `python3 compute/verify_lattice.py`. | Passed. Printed type-II Gram matrix `[[2,-2,-2],[-2,2,-2],[-2,-2,2]]`, `rho=(1,-1/2,1)`, and checked sample `alpha(n,l,m)` to `z`-pairing substitutions. | No independent primary-source check of Maass/GN reflection signs in this pass. | Add a concise lattice output table for `delta_i`, `rho`, and `alpha(n,l,m)` in manuscript notation. | Lattice arithmetic is stable. Keep automorphic character separate from compact orientation. |
| Active support cone: if `f(nm,l)!=0`, then `alpha(n,l,m)=n delta_1 + m delta_2 + (n+m-l)delta_3` lies in the nonnegative cone. | `main.tex:5396-5458`. | Bounded enumeration command using `phi_01_coefficients()` for `nm<=8`, `n,m<=8`; proof in `main.tex:5432-5458`. | Bounded check found no violations. Manuscript proof uses the weak Jacobi support bound `4nm-l^2>=-1`. | No general script proof; current proof is elementary and better than finite enumeration. | Add optional bounded regression check for active-support cone and the five generator triples in `compute/verify_lattice.py` or a new `verify_active_support.py`. | Keep proof. In report/table language say "active support" rather than all effective triples. |
| Height-four additive coefficients: `[q^2r^2s]`, `[qr^2s^2]`, `[qs]` in `D_5/(qrs)^{1/2}` equal `-90`; `[q^2r^2s^2]` equals `540`; hence `m=90,90,90,-540`. | `main.tex:13085-13108`; `compute/verify_square_root.py:257-266`, `439-446`. | `python3 compute/verify_square_root.py`. | Passed. Printed `(2,1,1):90`, `(1,2,1):90`, `(1,1,2):90`, `(2,2,2):-540`. | Does not compute lower PBW words or compact representatives. | Keep as target simple-root coefficient check. Do not extend `W_{\le3}` recognition by appending these numbers alone. |
| Height-four signed/additive gaps: signed target multiplicities are `108`; additive simple coefficients are `90`; residual is `18` in each of the three timelike height-four degrees. | `main.tex:13113-13132`, `main.tex:13189-13194`; `compute/verify_square_root.py:270-285`, `447-452`. | `python3 compute/verify_square_root.py`. | Passed. Printed `108|90|18` for `(2,1,1)`, `(1,2,1)`, `(1,1,2)`. | Full parity split and compact PBW decomposition remain uncomputed. | Add a canonical "signed | additive | residual" table to script output and cite it from the manuscript. | Use this as an obstruction to naive finite-window extension. |
| Doubled isotropic boundary rays: signed exponent `10`, GN isotropic simple multiplicity `tau=9`, residual `1`. | `main.tex:13134-13143`, `main.tex:13195-13198`; `compute/verify_square_root.py:287-300`, `453-458`. | `python3 compute/verify_square_root.py`. | Passed. Printed `10|9|1` for `(2,2,0)`, `(2,0,2)`, `(0,2,2)`. | No source primitive decomposition. | Add script table for doubled isotropic rays. | Keep as target convention check; not compact primitive recognition. |
| Primitive recognition first window `W_{\le3}` requires parity bases, bracket matrices, positive-negative pairings, radical kernels, coproduct matrices, transition maps, kernel equality, and PBW associated-graded isomorphism. | `main.tex:12479-12588`, `main.tex:12941-13083`, `main.tex:13205-13262`. | No executable compact-source data. Target dimensions checked by `verify_square_root.py`; NO7 matrix conditions are only specified. | Target arithmetic passed; source-side finite certificate not run because no matrices/bases exist. | Entire compact finite-window source certificate is open. | Add a data schema under `compute/` for finite windows: JSON/YAML or Python fixtures containing bases, parity blocks, `G_beta`, `K_beta`, `Q_beta`, bracket matrices `B_{alpha,beta}`, product/coproduct matrices, transition maps, and PBW ranks. Then add `verify_wle3_no7.py`. | Keep theorem conditional. Equality of displayed target dimensions is not recognition. |
| Normal-ordering finite diagrams `NO1`--`NO7` and closure criteria `N1`--`N7`. | `main.tex:9132-9319`, `main.tex:9654-9722`, `main.tex:13276-13336`. | No executable check. The manuscript lists the diagrams and matrix tests. | Not run; no finite Hall atlas, source matrices, or cochains supplied. | All chain-level finite normal-ordering computations remain unresolved: finite fibres, product/coproduct/pairing compatibility, Hochschild and negative-cyclic equations, triple flag, Jacobi, Frobenius, Hopf radical, transition closedness. | Add a finite-diagram verification harness separate from coefficient scripts. It should accept a finite stage `R`, successor `R+`, and matrices/cochains, then test `NO1`--`NO7`. | Do not state normal-ordering as chain-level constructed. The proved part is the lattice homomorphism and the specified certificate shape. |
| OP variable substitutions and scalar square: `q=q_OP`, `r=p_OP`, `s=tilde q_OP`; DT variables `q_DT=s`, `t_DT=q`, `p_DT=r`; `chi_10^OP=D_5^2=4096^{-1}Delta_5^2`; `Z_OP=-4096 Delta_5^{-2}`. | `main.tex:13716-13804`, `main.tex:13806-13855`; `proj.bib:249-286` for OB/OPand/Oberdieck. | No compute script. Local arithmetic `64^2=4096` and variable relabeling are direct. | Formula chain is internally consistent. No source-paper formula audit run by this agent. | Exact OB Theorem 1, OPand Section 2/Proposition 5, and Oberdieck Theorem 1/3(i) hypotheses still need primary-source audit if this section remains theorem-level. | Add a small `verify_op_normalization.py` that prints the variable dictionary and scalar conversion, but primary-source checks must be bibliographic/manual unless source TeX is vendored. | Keep OP as scalar, orientation-forgetful import. Never use the OP minus or `4096` as orientation/Pfaffian evidence. |
| Formal primitive lift of any Gram triple: `Q=e_1+n f_1`, `P=l f_1+e_2+m f_2` gives `Q^2=2n`, `P^2=2m`, `Q.P=l`, torsion-one wedge coefficient. | `main.tex:4523-4575`. | No dedicated script; arithmetic is elementary. `compute/verify_lattice.py` checks related `alpha`/pairing samples but not this exact lift. | Not separately run. | No compact effectivity/algebraicity/nonemptiness check; this is formal Mukai-lattice arithmetic only. | Add this lift and wedge-gcd check to `compute/verify_lattice.py` for representative triples, or a symbolic assertion. | Keep formal lift separate from compact Hall support. |
| Raw Gram descent no-go: strict fixed-lift raw `Pi_X` descent cannot realize type-II real-root strings; normal-ordered extension is forced. | `main.tex:4883-4968`; root-string constants backed by `compute/verify_square_root.py`. | Root constants checked by script; no source Hall bracket computation exists. | Numeric part passed. The theorem proof is TeX-local lattice algebra. | Fibre-summed raw construction is explicitly not excluded (`main.tex:4970-4975`). | Add a small lattice regression test for `B(c_i,c_i+c_j)=2 gamma_i` under fixed lifts if explicit lifts are chosen. | Keep the no-go theorem scoped to strict fixed-lift raw descent. |

## Commands Run

```bash
python3 compute/verify_square_root.py
python3 compute/verify_lattice.py
python3 - <<'PY'
from compute.verify_square_root import phi_01_coefficients, delta_pair, signed_root_supermultiplicity
phi = phi_01_coefficients()
for h in range(1, 4):
    for c1 in range(h + 1):
        for c2 in range(h - c1 + 1):
            c3 = h - c1 - c2
            beta = (c1, c2, c3)
            gamma = (c1, c1 + c2 - c3, c2)
            smult = signed_root_supermultiplicity(phi, beta)
            if smult:
                print(h, beta, gamma, delta_pair(beta, beta), smult)
PY
```

## Bottom Line

The target-side arithmetic is in good condition: the local scripts verify the displayed `phi_{0,1}` coefficients, `93`, `29|93`, real-string exponents, height-four residuals, doubled-isotropic residuals, and the type-II lattice matrix.  The computational gap is not the Borcherds/GN coefficient arithmetic.  The gap is executable source-side certification: finite normal-ordering diagrams, `W_{\le3}` compact bases, parity splits, Hall bracket/coproduct/pairing matrices, radical kernels, transition maps, PBW data, and OP primary-source formula audit.
