# A176 -- CYQG A168 Follow-Up Verifier

## Result

Read-only verification found that A168 was only partially closed.

## Findings

- High: `k3e_bkm_chapter.tex:1010`, `1054`, and `3641` still retained
  unsigned/root-multiplicity traps: \(\mathrm{mult}(\alpha)=|c(\cdots)|\)
  and \(\Omega(\gamma)=|c(D)|\).
- High: `k3_yangian_chapter.tex:1302` still said imaginary roots appear
  with multiplicity \(|c(\cdots)|\), and `6235` still said multiplicities
  \(|f(D(\alpha))|\).
- Medium: `compute/lib/k3e_coha_structure.py:820` still said the rank-0
  CoHA is freely generated by 24 generators.
- Low/medium: `root_multiplicity` was trap-fenced but not explicitly
  deprecated; nearby comments/tests still used `mult = c(D)` wording at
  `compute/lib/k3e_coha_structure.py:183` and
  `compute/tests/test_k3e_coha_structure.py:162`.

## Closed Items

- `k3_chiral_bialgebra_platonic.tex:3777` now uses signed protected index
  language and a parity-fixture caveat.
- `physics_bps_root_multiplicities.tex:745` now treats GV invariants as
  protected/refined and conditions ordinary generator counts on bridge and
  parity data.

## Checks

- Targeted `rg` over the six scoped files.
- `git diff --check -- <six scoped files>` passed.
- `pytest compute/tests/test_k3e_coha_structure.py -q` returned
  `92 passed in 5.99s`.

## Main-Thread Follow-Up

The main thread patched the CoHA docstring/comment/test wording locally.
The BKM and Yangian files were assigned to A180 and A181.

