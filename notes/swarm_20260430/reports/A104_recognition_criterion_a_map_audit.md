# A104 recognition criterion A-map audit

No files edited by the agent.

## Verdict before master-thread patch

The target-label firewall was present, but the executable finite source
matrix criterion did not yet itself require comparison maps
\(A_{\beta,\bar p}\) or their intertwining equations. The maps existed in
the earlier finite comparison-map proposition, but not as a clause of the
executable criterion.

## Required closure

The criterion must choose parity-preserving source-to-target
isomorphisms
\[
A_{\beta,\bar p}:L_{\beta,\bar p}\xrightarrow{\sim}
\mathfrak g_{\Delta_5,\beta,\bar p},
\qquad A_0=\iota_H,
\]
for every retained \(\beta\), compatible with HN/window transitions.
It must require bracket, coproduct, and pairing intertwining:
\[
A_{\alpha+\beta}QB^X_{\alpha,\beta}
=B^\Delta_{\alpha,\beta}(AQ\otimes AQ),
\]
\[
(A\otimes A)(Q\otimes Q)D^X=D^\Delta AQ,
\qquad
G^\Delta(Ax,Ay)=G^X(x,y).
\]

It must also bind the target-label firewall inside the criterion: source
row and column labels are compact-source labels; target labels occur only
in \(B^\Delta,D^\Delta,G^\Delta\) or codomain coordinates of
\(A_{\beta,\bar p}\).

The master thread patched these clauses after the report.

