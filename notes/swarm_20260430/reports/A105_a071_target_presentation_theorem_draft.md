# A105 A071 target-presentation theorem draft

No files edited by the agent.

## Insertion anchor

After the proof of `prop:first-saturated-primitive-recognition-window`,
before `thm:finite-compact-source-provenance-gate`.

## Proposed theorem content

Insert a target-only proposition proving the A071 target parity rows that
are already supported by Kac/GN:

- \(2a_{ij}:10|0\), using the \(A_1^{(1)}\) rank-two real subalgebra and
  the nine GN isotropic simple directions.
- \(C_{k,3}:29|93\), by real Weyl transport from \(\delta_{123}:29|93\).
- \(C_{k,4}:10|0\), by real Weyl transport from \(a_{ij}:10|0\).
- \(C_{k,5}:0|0\), by Kac real-root string termination.

Leave \(C_{k,2}\) and \(2\delta_{123}\) signed-only pending a finite
target presentation reducer.

## Citations

Use `\cite[Sections~3--4, Proposition~3.1]{GN}` for the automorphic
correction algebra, `\cite{BorcherdsGKM88}` for generalized
Kac--Moody presentation conventions, and `\cite{KAC:1}` for real-root
Weyl action, root strings, and PBW conventions.

## Warning

This proposition is target-presentation arithmetic only. It must not be
cited as source recognition: it supplies no compact Hall representatives,
source pairings, radicals, \(M,D,B,G,K,Q\) matrices, no-extra theorem, or
PBW comparison.

