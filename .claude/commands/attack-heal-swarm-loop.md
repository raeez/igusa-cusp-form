---
description: "Self-pacing /loop swarm: relaunch stopped agents, maintain the user-requested attack-heal swarm size across all research volumes"
model: opus
---

/loop 3m first relaunch any rate limited or stopped agents. Maintain the user-requested swarm size across ALL RESEARCH VOLUMES (default 5 only when no target is specified; larger targets such as 30+ are allowed when explicitly requested). If the active swarm is below that requested target, launch enough hardcore convergent ATTACK AND THEN HEAL loop agents to restore the requested size across /Users/raeez/chiral-bar-cobar, /Users/raeez/chiral-bar-cobar-vol2, /Users/raeez/calabi-yau-quantum-groups, /Users/raeez/igusa-cusp-form, and /Users/raeez/topological-strings.

Each agent must attack the mathematical assertions, formulas, status tags, derivations, and cross-repository correspondences from first principles; then heal only the surviving core into theorem, proof, conjecture, computation, or frontier form. Assign disjoint scopes and explicit write ownership. Agents must not revert work they did not make.

Require proof-grade reports: claim attacked, failure mode or proof, file anchors, primary-source anchors where needed, exact constants and formulas, changed files, verification run, and remaining open questions. The loop only converges when no new fatal attack survives and at least one real mathematical improvement has been inscribed.

After every wave completes (all requested agents reported), integrate by deep semantic merge across all affected repositories. Preserve mathematically substantive content from every side, read conflicts in context, and use Raeez Lorgat authorship only. No AI attribution, no git stash, and no commit amend without explicit instruction.

Only retain one /loop cron; if a prior one exists, delete it before creating the new one.
